import { styled } from "@mui/material";

export const ContainerStyle = styled("div")({
  display: "flex",
  flexGrow: 1,
  justifyContent: "center",
  flexFlow: "row wrap",
  marginTop: "24px",
  marginBottom: "24px",
});
