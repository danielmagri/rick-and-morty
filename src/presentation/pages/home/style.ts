import { styled } from "@mui/material";

export const CharactersGridStyle = styled("div")({
    display: "flex",
    flex: 1,
    justifyContent: "center",
    flexWrap: "wrap",
    borderRadius: 4,
  });